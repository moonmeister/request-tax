## Thesis

HTTP/3's multiplexed streams eliminate the **per-request tax** that forces developers to bundle assets.

With H3, splitting payloads into many small requests should be _free or faster_ than monolithic downloads.

But does that hold across payload sizes, network conditions, and caching strategies?
